import styled from "styled-components";

export const VendorListStyle = styled.div`
  border: 1px dashed var(--secondary-color);
  padding: 2rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
  text-align: center;
  border-radius: 1rem;

  span {
    color: var(--primary-color);
  }
`;
