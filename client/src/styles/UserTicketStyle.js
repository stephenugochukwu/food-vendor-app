import styled from "styled-components";

export const UserTicketStyle = styled.div`
  border: 1px dashed var(--primary-color);
  padding: 2rem;
  text-align: center;
  border-radius: 1rem;

  span {
    color: var(--primary-color);
  }
`;
